{
  "name": "we-finance",
  "type": "module",
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "clean": "rimraf --glob \"apps/*/{node_modules,dist}\" \"node_modules\" \".turbo\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "rimraf": "^6.0.1"
  }
}
